# 53 奇門遁甲 1080.md unknown token 完整修正清單

本文件是新第 91 包 docs-only 清單，目的不是修 `data/1080.md`，而是重跑 `qimen1080MarkdownParser` 後，列出目前剩餘 `UNKNOWN_*` diagnostics，提供後續人工逐筆對照手修。

本包不修改 parser、不修改 tests、不修改 UI、不產生 JSON，也不寫入 `data/qimen/plates/**`。

## 1. 本包目的

本包只整理目前剩餘 unknown token：

* `UNKNOWN_DOOR`
* `UNKNOWN_DEITY`
* `UNKNOWN_STEM`

清單包含 code、遁別、局數、日群、時柱、宮位、raw cell、parser 訊息，以及建議人工判讀方向。清單中的建議只作為判讀提示，不代表可以直接批次替換。

## 2. 本輪重跑後 stats

| 指標 | 本輪 |
|---|---:|
| `totalPlates` | 1079 |
| `yangPlates` | 539 |
| `yinPlates` | 540 |
| `errors` | 163 |
| `warnings` | 0 |

補充觀察：本輪 parser 顯示總盤數回到 1079，陽遁四局目前為 59 個唯一時柱，缺少 `戊戌`，沒有重複時柱。這不屬於本清單的 `UNKNOWN_*` 範圍，但下一輪驗證時應一併追查。

## 3. `UNKNOWN_*` code count

| code | count |
|---|---:|
| `UNKNOWN_DOOR` | 8 |
| `UNKNOWN_DEITY` | 5 |
| `UNKNOWN_STEM` | 0 |

## 4. `UNKNOWN_DOOR` 完整清單

| # | 遁別 | 局 | 日群 | 時柱 | 宮位 | raw cell / 原始文字 | parser 訊息 | 建議人工判讀方向 |
|---:|---|---:|---|---|---|---|---|---|
| 1 | 陽遁 | 1 | 丙辛日 | 癸巳 | `zhen` | `己 芮<br>庚 壬 合` | 未知八門：`壬` | 第二行應為地盤干 + 八門 + 八神；`壬` 是天干，不是八門，需回查此宮八門是否缺字或錯位。 |
| 2 | 陽遁 | 4 | 丙辛日 | 丁酉 | `kun` | `戊 輔<br>丙 柱 雀` | 未知八門：`柱` | `柱` 是九星簡稱，不是八門；需確認第二行八門是否誤植為星名。 |
| 3 | 陽遁 | 4 | 丁壬日 | 辛亥 | `kun` | `丁 蓬<br>丙 天 開` | 未知八門：`天` | `天` 比較像八神簡稱，`開` 是八門；優先檢查第二行是否應為 `丙 開 天`。 |
| 4 | 陽遁 | 9 | 丁壬日 | 戊申 | `gen` | `己 蓬<br>乙 柱 雀` | 未知八門：`柱` | `柱` 是九星簡稱，不是八門；需回查此宮八門，避免直接用星名硬改。 |
| 5 | 陽遁 | 9 | 丁壬日 | 庚戌 | `zhen` | `己 蓬<br>辛 雀 死` | 未知八門：`雀` | `雀` 是八神簡稱，`死` 是八門；優先檢查第二行是否應為 `辛 死 雀`。 |
| 6 | 陰遁 | 9 | 乙庚日 | 己卯 | `kun` | `丙 芮<br>丙 芮 陳` | 未知八門：`芮` | `芮` 是九星簡稱，不是八門；需確認第二行八門是否被誤植為星名。 |
| 7 | 陰遁 | 6 | 乙庚日 | 丁亥 | `li` | `庚 輔<br>丁 符 開` | 未知八門：`符` | `符` 是八神簡稱，`開` 是八門；優先檢查第二行是否應為 `丁 開 符`。 |
| 8 | 陰遁 | 5 | 丁壬日 | 癸卯 | `xun` | `癸 英<br>己 柱 蛇` | 未知八門：`柱` | `柱` 是九星簡稱，不是八門；需回查此宮八門是否漏字或錯位。 |

## 5. `UNKNOWN_DEITY` 完整清單

| # | 遁別 | 局 | 日群 | 時柱 | 宮位 | raw cell / 原始文字 | parser 訊息 | 建議人工判讀方向 |
|---:|---|---:|---|---|---|---|---|---|
| 1 | 陽遁 | 4 | 丁壬日 | 辛亥 | `kun` | `丁 蓬<br>丙 天 開` | 未知八神簡稱：`開` | `開` 是八門，不是八神；與同 cell 的 `UNKNOWN_DOOR: 天` 配對判讀，優先檢查是否門神順序顛倒。 |
| 2 | 陽遁 | 9 | 甲己日 | 癸酉 | `kan` | `丁 心<br>己 休 休` | 未知八神簡稱：`休` | `休` 是八門；需確認第三 token 應為哪一個八神，勿直接把第二個 `休` 當八神。 |
| 3 | 陽遁 | 9 | 丁壬日 | 庚戌 | `zhen` | `己 蓬<br>辛 雀 死` | 未知八神簡稱：`死` | `死` 是八門，不是八神；與同 cell 的 `UNKNOWN_DOOR: 雀` 配對判讀，優先檢查是否門神順序顛倒。 |
| 4 | 陰遁 | 6 | 乙庚日 | 丁亥 | `li` | `庚 輔<br>丁 符 開` | 未知八神簡稱：`開` | `開` 是八門，不是八神；與同 cell 的 `UNKNOWN_DOOR: 符` 配對判讀，優先檢查是否門神順序顛倒。 |
| 5 | 陰遁 | 5 | 乙庚日 | 己卯 | `gen` | `丁 任<br>丁 杜 生` | 未知八神簡稱：`生` | `生` 是八門，不是八神；第二 token `杜` 已是八門，需回查第三 token 應是哪個八神。 |

## 6. `UNKNOWN_STEM` 完整清單

本輪 `UNKNOWN_STEM` 為 0，沒有剩餘清單。

## 7. 手工修正判讀原則

* 外宮第一行應為：天盤干 + 九星。
* 外宮第二行應為：地盤干 + 八門 + 八神。
* 中宮不可用外宮規則硬修，需保留天禽 / 寄宮可能性。
* 不要做全域替換；同一個字在不同欄位可能代表不同錯位原因。
* 不確定者只標記，不要猜改。
* 若同一 raw cell 同時出現 `UNKNOWN_DOOR` 與 `UNKNOWN_DEITY`，優先判斷第二行門神是否順序顛倒。
* 若 unknown token 是天干或九星簡稱，通常代表該 token 落在八門欄位，需要回查原盤面而非直接套門名。

## 8. 建議下一輪手修順序

1. 先處理同一 raw cell 同時有 `UNKNOWN_DOOR` / `UNKNOWN_DEITY` 的門神順序疑似顛倒案例：`丁 蓬<br>丙 天 開`、`己 蓬<br>辛 雀 死`、`庚 輔<br>丁 符 開`。
2. 再處理 unknown token 明顯是九星簡稱落入八門欄位的案例：`柱`、`芮`。
3. 處理 `UNKNOWN_DEITY` 但第二 token 已是合法八門的案例，例如 `丁 任<br>丁 杜 生`。
4. 最後回查 `己 芮<br>庚 壬 合`，因為 `壬` 是天干，可能是欄位錯位、漏門，或來源資料誤植。
5. 另行追查陽遁四局缺少 `戊戌` 盤，讓總盤數回到 1080。

每修一小批後，建議重跑 parser diagnostics，避免 unknown token 修正造成新的盤數或元素缺漏問題。

## 9. 本包不處理項目

* 不修改 `data/1080.md`。
* 不修改 parser。
* 不修改 tests。
* 不修改 UI。
* 不產生 JSON。
* 不寫入 `data/qimen/plates/**`。
* 不做 converter。
* 不做格局、卦名、斷語、吉凶。
* 不 commit。

## 10. 本文件結論

本輪重跑後剩餘 unknown token 已收斂為 13 筆：

* `UNKNOWN_DOOR`: 8
* `UNKNOWN_DEITY`: 5
* `UNKNOWN_STEM`: 0

這 13 筆多數集中在外宮第二行的八門 / 八神欄位錯位或疑似誤植。下一輪應逐筆人工核對，不要全域替換；同時需注意目前 parser stats 顯示總盤數為 1079，陽遁四局缺 `戊戌`，應在後續驗證中追查。
