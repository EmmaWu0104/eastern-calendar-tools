# 34 奇門遁甲年度完整循環草案交接摘要

## 1. 本階段定位

本階段是傳統置閏法 resolver 的草案 pipeline 收斂。目前仍未接 UI，未接 1080 盤面 JSON，未擴大 `INITIAL_QIMEN_TIMELINE`，未改 `resolveQimenJu` 主流程，也未取代既有 2027 fixture / 初版 resolver。

這一階段只建立「年度窗口分析 -> full cycle draft input -> full cycle timeline draft」的 helper 與測試，作為後續完整年度奇門 timeline 的 future path。

## 2. 第 25 包完成內容

第 25 包新增：

```js
buildQimenFullTermCycleDraftInputForYear(year, options = {})
```

此 helper 使用 `analyzeQimenIntercalationWindowsForYearAuto(year)`，第一版只支援 `startTerm = "大雪"`，回傳：

- `year`
- `startSeed`
- `intercalations`
- `windows`

2027 案例：

- `startSeed` = 大雪 `2027-11-25T23:00:00+08:00`
- `intercalation` = 大雪 `2027-12-10T23:00:00+08:00`
- `windows` = 芒種 / 大雪

此 helper 不自行重新判斷置閏日數，只使用年度窗口分析結果，也不接 resolver 主線。

## 3. 第 26 包完成內容

第 26 包新增：

```js
buildQimenFullTermCycleTimelineDraftForYear(year, options = {})
```

此 helper 內部使用 `buildQimenFullTermCycleDraftInputForYear(year, options)`，再使用 `buildQimenTimelineFromFullTermSeedCycle(...)`，回傳：

- `year`
- `startSeed`
- `intercalations`
- `windows`
- `timeline`

2027 會產生 75 筆 full cycle timeline 草案：

- 24 個正常節氣 x 3 元 = 72 筆
- 1 個閏大雪 x 3 元 = 3 筆
- 合計 75 筆

測試已驗證其與 direct draft input + full cycle timeline helper 組合結果一致，也已驗證與 2027 fixture 大雪段對齊。

## 4. 目前 helper 關係

草案 pipeline：

```text
analyzeQimenIntercalationWindowsForYearAuto(year)
-> buildQimenFullTermCycleDraftInputForYear(year)
-> buildQimenTimelineFromFullTermSeedCycle(...)
-> buildQimenFullTermCycleTimelineDraftForYear(year)
```

既有主線仍是：

```text
buildQimenYearSeedRecommendations(2027)
-> buildQimenTimelineFromYearSeedRecommendations(2027)
-> buildSeedDrivenQimenTimelineFixture2027()
-> INITIAL_QIMEN_TIMELINE
-> resolveQimenJu(...)
```

草案 pipeline 尚未取代主線。目前它只是可測試、可比對的 future path。

草案串接 pipeline：

```text
buildQimenFullTermCycleTimelineDraftForYear(year)
-> buildQimenMultiYearFullTermCycleTimelineDraft({ startYear, endYear })
-> diagnostics duplicate / gap / overlap check
```

多年串接 helper 尚未取代主線。它只是 future resolver timeline window 的候選基礎。

lookup 草案 pipeline：

```text
findQimenFullTermCycleTimelineDraftEntry(dateTimeText)
-> getQimenEffectiveDayStart(dateTimeText)
-> candidateYear / candidateYear - 1 / candidateYear + 1
-> buildQimenFullTermCycleTimelineDraftForYear(year)
-> selected timeline entry
```

這條 pipeline 尚未接 `resolveQimenJu()`。它只是 future resolver replacement 的 lookup strategy 實驗。

formatter 草案 pipeline：

```text
resolveQimenJuFromFullTermCycleDraft(dateTimeText)
-> findQimenFullTermCycleTimelineDraftEntry(dateTimeText)
-> find actual solar term
-> getQimenYuanJu(qimenSolarTerm, yuan)
-> getHourPillar(...)
-> resolveQimenStatus(...)
-> draft resolver-like result + lookup metadata
```

這條 pipeline 尚未接 UI，也尚未替換 `resolveQimenJu()`。它是 future resolver replacement 前的 read-only comparison path。

## 5. 已驗證測試

目前新增測試輸出：

- 奇門完整循環草案輸入測試通過：6 cases
- 奇門年度完整循環Timeline草案測試通過：6 cases
- 奇門年度完整循環Timeline跨年草案測試通過：2 cases
- 奇門年度完整循環Timeline多年觀察測試通過：7 cases
- 奇門多年完整循環Timeline草案串接測試通過：7 cases
- 奇門多年完整循環Timeline全範圍diagnostics測試通過：1 cases
- 奇門多年完整循環Timeline duplicate detail diagnostics測試通過：1 cases
- 奇門完整循環Timeline草案查詢測試通過：8 cases
- 奇門完整循環Timeline草案duplicate boundary查詢測試通過：1 cases
- 奇門完整循環Timeline草案查詢與resolver對齊測試通過：9 cases
- 奇門完整循環草案resolver formatter測試通過：9 cases
- 奇門完整循環草案resolver formatter regression測試通過：14 cases

既有測試仍需維持：

- 奇門年度Seed建議測試
- 奇門年度Seed建議Timeline測試
- 奇門2027 Seed fixture測試
- 奇門置閏法 resolver 初版測試

## 6. 2027 代表性驗證點

- 大雪起點：`2027-11-25T23:00:00+08:00`
- 閏大雪起點：`2027-12-10T23:00:00+08:00`
- 冬至起點：`2027-12-25T23:00:00+08:00`

大雪段：

- 2027-11-25 大雪上元 false
- 2027-11-30 大雪中元 false
- 2027-12-05 大雪下元 false

閏大雪段：

- 2027-12-10 大雪上元 true
- 2027-12-15 大雪中元 true
- 2027-12-20 大雪下元 true

冬至段：

- 2027-12-25 冬至上元 false

## 7. 跨年份草案驗證結果

第 28 包新增 2026 / 2028 年度 full cycle draft 草案驗證測試，測試輸出：

- 奇門年度完整循環Timeline跨年草案測試通過：2 cases

2026 驗證結果：

- timeline length = 72
- intercalations = 0
- 大雪 `startSeed` = `2026-11-30T23:00:00+08:00`
- 可產生大雪起點 `startSeed`
- timeline start 遞增且不重複
- 無置閏，因此無閏節氣三元需比對

2028 驗證結果：

- timeline length = 72
- intercalations = 0
- 大雪 `startSeed` = `2028-12-04T23:00:00+08:00`
- 可產生大雪起點 `startSeed`
- timeline start 遞增且不重複
- 無置閏，因此無閏節氣三元需比對

目前觀察：

- 2026 / 2028 都可用大雪起點產生完整 72 筆年度 full cycle timeline 草案。
- 2027 則是 75 筆，原因是多了閏大雪三元。
- 目前三年觀察為：
  - 2026：72 筆，無置閏
  - 2027：75 筆，閏大雪
  - 2028：72 筆，無置閏

以上只能表示 2026 / 2027 / 2028 三年草案驗證通過，不代表大雪起點已證明適用所有年份。任意年份 anchor 策略仍待後續驗證。

### 7.1 2024～2030 多年觀察

第 30 包新增 2024～2030 年度 full cycle draft 草案觀察測試，測試輸出：

- 奇門年度完整循環Timeline多年觀察測試通過：7 cases

逐年觀察表：

| 年份 | 大雪 startSeed | 置閏數 | timeline 筆數 | 置閏資料 |
|---|---|---:|---:|---|
| 2024 | `2024-11-25T23:00:00+08:00` | 1 | 75 | 閏大雪 `2024-12-10T23:00:00+08:00` |
| 2025 | `2025-12-05T23:00:00+08:00` | 0 | 72 | 無 |
| 2026 | `2026-11-30T23:00:00+08:00` | 0 | 72 | 無 |
| 2027 | `2027-11-25T23:00:00+08:00` | 1 | 75 | 閏大雪 `2027-12-10T23:00:00+08:00` |
| 2028 | `2028-12-04T23:00:00+08:00` | 0 | 72 | 無 |
| 2029 | `2029-11-29T23:00:00+08:00` | 0 | 72 | 無 |
| 2030 | `2030-11-24T23:00:00+08:00` | 1 | 75 | 閏大雪 `2030-12-09T23:00:00+08:00` |

目前觀察：

- 2024～2030 測試中，每年都可產生大雪起點 full cycle draft timeline。
- timeline 筆數皆符合 `72 + intercalations.length * 3`。
- 目前觀察到的置閏都發生在大雪窗口，且均為單一閏大雪。
- 測試中未出現 `intercalations.length > 1` 的情況。
- 這仍然只是 2024～2030 的觀察，不能推論所有年份。

## 8. 多年 timeline 串接草案

### 8.1 新增 helper

第 32 包新增：

```js
buildQimenMultiYearFullTermCycleTimelineDraft({
  startYear,
  endYear,
})
```

用途：

- 依年份範圍產生多年度 full cycle draft timeline 草案。
- 內部使用 `buildQimenFullTermCycleTimelineDraftForYear(year)`。
- 合併每年的 `timeline`。
- 依 `start` 升冪排序。
- 對重複 start 先保留第一筆，並在 diagnostics 中記錄。
- 這仍是 read-only / draft helper。
- 尚未接 `INITIAL_QIMEN_TIMELINE`。
- 尚未接 `resolveQimenJu()`。
- 尚未接 UI。

回傳格式：

```js
{
  startYear,
  endYear,
  yearDrafts,
  timeline,
  diagnostics
}
```

其中 `diagnostics` 包含：

```js
{
  yearCount,
  entryCountBeforeDedupe,
  entryCountAfterDedupe,
  duplicateStarts,
  overlaps,
  gaps
}
```

### 8.2 新增測試

新增測試輸出：

- 奇門多年完整循環Timeline草案串接測試通過：7 cases

測試涵蓋：

- 2024～2030 串接
- 2026～2028 串接
- 2027 單年與 `buildQimenFullTermCycleTimelineDraftForYear(2027).timeline` 對齊
- invalid startYear type
- invalid endYear type
- invalid range
- missing year data

### 8.3 串接 diagnostics 觀察結果

2024～2030：

- entryCountBeforeDedupe = 513
- entryCountAfterDedupe = 513
- duplicateStarts = 0
- gaps = 0
- overlaps = 0

2026～2028：

- entryCountBeforeDedupe = 219
- entryCountAfterDedupe = 219
- duplicateStarts = 0
- gaps = 0
- overlaps = 0

2027 單年：

- timeline = 75
- duplicateStarts = 0
- gaps = 0
- overlaps = 0
- 與 `buildQimenFullTermCycleTimelineDraftForYear(2027).timeline` 對齊

目前 2024～2030 多年 draft timeline 串接沒有出現缺口、重疊或重複 start。這代表在目前測試範圍內，多年度草案 timeline 可以形成連續序列。但這仍然只是 2024～2030 的觀察，尚未驗證 1899～2101 全資料範圍。

### 8.4 1899～2101 full range diagnostics

第 34 包新增 full range diagnostics 測試，測試輸出：

- 奇門多年完整循環Timeline全範圍diagnostics測試通過：1 cases

實際掃描範圍：

- 1899～2101
- totalYears = 203

diagnostics 結果：

| 項目 | 數值 |
|---|---:|
| entryCountBeforeDedupe | 14898 |
| entryCountAfterDedupe | 14829 |
| duplicateStarts.length | 69 |
| gaps.length | 0 |
| overlaps.length | 0 |

第一筆 duplicate：

- start = `1910-11-24T23:00:00+08:00`
- count = 2

年度置閏統計：

| 項目 | 數值 |
|---|---:|
| yearsWithIntercalation.length | 94 |
| yearsWithoutIntercalation.length | 109 |
| maxIntercalationsPerYear | 1 |
| yearsWithMultipleIntercalations.length | 0 |

sanity check：

- 2024：1 個置閏，閏大雪 `2024-12-10T23:00:00+08:00`
- 2025：0 個置閏
- 2027：1 個置閏，閏大雪 `2027-12-10T23:00:00+08:00`
- 2030：1 個置閏，閏大雪 `2030-12-09T23:00:00+08:00`

full range 掃描沒有 gaps，也沒有 overlaps。full range 有 69 個 duplicate starts，推測屬於多年 full cycle 串接時的年度邊界重複。目前 diagnostics helper 會先保留第一筆 entry，並記錄 duplicateStarts。這支持後續可以設計去重 / 查詢 window 策略。

因為 duplicateStarts 不為 0，目前還不應直接替換 `INITIAL_QIMEN_TIMELINE` 或接 `resolveQimenJu()`。

### 8.5 duplicate detail diagnostics

第 36 包新增 duplicate detail diagnostics 測試，測試輸出：

- 奇門多年完整循環Timeline duplicate detail diagnostics測試通過：1 cases

此測試從 `yearDrafts` 還原 raw timeline，依 `start` 分組檢查 duplicate entries，沒有修改 `src/qimenResolver.js`、去重規則或 resolver 主線。

觀察結果：

| 項目 | 數值 |
|---|---:|
| duplicate group 數量 | 69 |
| 全部 group entries.length = 2 | 是 |
| equivalentDuplicateGroups.length | 0 |
| differentDuplicateGroups.length | 69 |
| adjacentYearDuplicateGroups.length | 69 |
| nonAdjacentYearDuplicateGroups.length | 0 |

第一筆 duplicate group：

| 欄位 | 值 |
|---|---|
| start | `1910-11-24T23:00:00+08:00` |
| years | `1909 / 1910` |
| qimenSolarTerm | `小雪 / 大雪` |
| yuan | `上元` |
| end | `1910-11-29T23:00:00+08:00` |
| isIntercalary | `false` |
| sourceDayPillar | `甲午` |

69 筆 duplicate group 全部來自相鄰年度，但都不是完全等價。目前第一筆確認差異欄位為 `qimenSolarTerm`。因為 duplicate entries 可能代表不同年度 cycle 對同一有效日的不同節氣標籤，不能簡單宣稱 duplicate 可任意去重。

目前 `buildQimenMultiYearFullTermCycleTimelineDraft(...)` 仍然只是 diagnostics / draft helper，不應直接把 deduped timeline 接到 resolver 主線。下一步應該先設計查詢 window 邏輯或 anchor 選擇策略，而不是直接採用「保留第一筆」。

### 8.6 read-only lookup strategy 草案

第 38 包新增 read-only helper：

```js
findQimenFullTermCycleTimelineDraftEntry(dateTimeText, options = {})
```

第一版支援：

```js
{
  strategy: "cycle-year"
}
```

用途：

- 依查詢時間找出 full cycle draft timeline entry。
- 不使用 `INITIAL_QIMEN_TIMELINE`。
- 不影響 `resolveQimenJu()`。
- 不接 UI。
- 不讀取 1080 盤面 JSON。
- 作為後續 resolver 替換前的 lookup strategy 草案。

#### 8.6.1 cycle-year strategy

策略流程：

1. 使用 `getQimenEffectiveDayStart(dateTimeText)` 取得奇門有效日 23:00 起點。
2. 以查詢時間的台北 civil year 作為 candidate year。
3. 先查 candidate year 的 `buildQimenFullTermCycleTimelineDraftForYear(year)`。
4. 若該 yearDraft 覆蓋查詢有效日，回傳該 entry。
5. 若沒有命中，再依序嘗試 `candidateYear - 1`、`candidateYear + 1`。
6. 若仍沒有命中，回傳 `null`。
7. 若 strategy 不支援，throw `RangeError`。
8. 若年份資料缺失，沿用底層 helper 的 `RangeError`。

回傳 metadata：

```js
lookup: {
  strategy: "cycle-year",
  queryEffectiveDayStart,
  selectedYear,
  candidateYears
}
```

`candidateYears` 是實際嘗試過的年份。這個 helper clone entry 後回傳，不修改原始 timeline entry。

#### 8.6.2 新增測試

新增測試輸出：

- 奇門完整循環Timeline草案查詢測試通過：8 cases

測試涵蓋：

- 1910 duplicate start boundary
- 1910 duplicate start 前一刻
- 1910 duplicate start 後一天
- 2027 閏大雪
- 2027 冬至上元
- 2028 年初跨 cycle fallback
- invalid strategy
- missing supported data

#### 8.6.3 查詢觀察結果

| 查詢時間 | selectedYear | qimenSolarTerm | yuan | start | end | isIntercalary | sourceDayPillar | candidateYears |
|---|---:|---|---|---|---|---|---|---|
| `1910-11-24T23:30:00+08:00` | 1910 | 大雪 | 上元 | `1910-11-24T23:00:00+08:00` | `1910-11-29T23:00:00+08:00` | false | 甲午 | `[1910]` |
| `1910-11-24T22:30:00+08:00` | 1909 | 立冬 | 下元 | `1910-11-19T23:00:00+08:00` | `1910-11-24T23:00:00+08:00` | false | 己丑 | `[1910, 1909]` |
| `1910-11-25T12:00:00+08:00` | 1910 | 大雪 | 上元 | `1910-11-24T23:00:00+08:00` | `1910-11-29T23:00:00+08:00` | false | 甲午 | `[1910]` |
| `2027-12-11T12:00:00+08:00` | 2027 | 大雪 | 上元 | `2027-12-10T23:00:00+08:00` | `2027-12-15T23:00:00+08:00` | true | 甲子 | `[2027]` |
| `2027-12-26T12:00:00+08:00` | 2027 | 冬至 | 上元 | `2027-12-25T23:00:00+08:00` | `2027-12-30T23:00:00+08:00` | false | 己卯 | `[2027]` |
| `2028-01-01T12:00:00+08:00` | 2027 | 冬至 | 中元 | `2027-12-30T23:00:00+08:00` | `2028-01-04T23:00:00+08:00` | false | 甲申 | `[2028, 2027]` |

`1910-11-24T23:30` 命中 1910 draft 的大雪上元，避免全域 dedupe 保留 1909 小雪 entry 的問題。`1910-11-24T22:30` 還沒進入 23:00 有效日，因此命中 1909 draft 的立冬下元。`2028-01-01` 以 2028 為 candidate year 時未命中，fallback 到 2027 draft，符合跨 cycle 查詢需求。

這支持「查詢時先選 yearDraft / cycle window」比「全域合併後直接 dedupe」更安全。但這仍只是草案 helper，尚未替換 resolver 主線。

### 8.7 duplicate boundary lookup 自動化測試

第 40 包新增 69 組 duplicate boundary lookup 自動化測試，測試輸出：

- 奇門完整循環Timeline草案duplicate boundary查詢測試通過：1 cases

此測試：

- 沿用第 36 包從 `yearDrafts` 還原 duplicate group 的方式。
- 使用 1899～2101 full range。
- 針對 69 組 duplicate start 自動測試 boundary after / boundary before 查詢。
- 未修改 `findQimenFullTermCycleTimelineDraftEntry(...)`。
- 未修改 `src/qimenResolver.js`、resolver 主線、去重規則、data 或 UI。

#### 8.7.1 統計結果

| 項目 | 數值 |
|---|---:|
| duplicateGroups.length | 69 |
| boundaryAfterSelectedCurrentYearCount | 69 |
| boundaryAfterMismatchCount | 0 |
| boundaryBeforeSelectedPreviousYearCount | 23 |
| boundaryBeforeSelectedCurrentYearCount | 46 |
| boundaryBeforeOtherSelectedYearCount | 0 |

boundary after 指 duplicate start 當天 23:30。boundary before 指 duplicate start 當天 22:30。

69 組 boundary after 全部命中 current year draft，代表 23:00 生效後 `cycle-year` strategy 行為穩定。boundary before 會分成 previous year 23 組與 current year 46 組，但沒有任何案例落到其他年份。因此 boundary before 雖然不是單一規則，但仍限於相鄰年度 cycle 範圍內。

這支持 `cycle-year` strategy 可作為後續 lookup strategy 的候選基礎，但仍不代表可以直接替換 resolver。

#### 8.7.2 1910 sanity check

after query：

- 查詢：`1910-11-24T23:30:00+08:00`
- selectedYear：1910
- qimenSolarTerm：大雪
- yuan：上元
- sourceDayPillar：甲午

before query：

- 查詢：`1910-11-24T22:30:00+08:00`
- selectedYear：1909
- qimenSolarTerm：立冬
- yuan：下元
- sourceDayPillar：己丑
- start：`1910-11-19T23:00:00+08:00`
- end：`1910-11-24T23:00:00+08:00`

#### 8.7.3 觀察結論

第 40 包確認：在 duplicate start 23:00 生效後，`cycle-year` strategy 對 69 組 duplicate boundary 都會選 current year draft。這解決了「全域 dedupe 可能保留 previous year entry」的風險。

boundary before 的 23 / 46 分布表示，23:00 前一刻仍需依實際 yearDraft 覆蓋區間判斷，不應硬套單一 previous-year 規則。

目前策略仍是 read-only 草案；下一步應做「draft lookup result 與既有 2027 resolver 初版結果比對」，或補文件後再做接近 resolver 格式的 draft result helper。仍不可直接替換 `INITIAL_QIMEN_TIMELINE` 或 `resolveQimenJu()`。

### 8.8 2027 resolver alignment 測試

第 42 包新增 2027 既有 resolver 初版結果 vs draft lookup entry 對齊測試，測試輸出：

- 奇門完整循環Timeline草案查詢與resolver對齊測試通過：9 cases

此測試：

- 使用既有 `resolveQimenJu(input)`。
- 使用第 38 包新增的 `findQimenFullTermCycleTimelineDraftEntry(input)`。
- 只比對 timeline entry 層級欄位：
  - `qimenSolarTerm`
  - `yuan`
  - `isIntercalary`
- 不比對完整 resolver output：
  - `actualSolarTerm`
  - `status`
  - `dunType`
  - `dunName`
  - `ju`
  - `hourPillar`
  - `notes`
- 未修改 `findQimenFullTermCycleTimelineDraftEntry(...)`。
- 未修改 `src/qimenResolver.js`、`INITIAL_QIMEN_TIMELINE` 或 `resolveQimenJu()` 主流程。

#### 8.8.1 對齊結果

| 查詢時間 | selectedYear | qimenSolarTerm | yuan | isIntercalary | 是否與 resolver 對齊 |
|---|---:|---|---|---|---|
| `2027-06-06T12:00:00+08:00` | 2026 | 芒種 | 中元 | false | 是 |
| `2027-06-13T12:00:00+08:00` | 2026 | 芒種 | 下元 | false | 是 |
| `2027-06-14T12:00:00+08:00` | 2026 | 夏至 | 上元 | false | 是 |
| `2027-12-07T18:00:00+08:00` | 2027 | 大雪 | 下元 | false | 是 |
| `2027-12-11T12:00:00+08:00` | 2027 | 大雪 | 上元 | true | 是 |
| `2027-12-16T12:00:00+08:00` | 2027 | 大雪 | 中元 | true | 是 |
| `2027-12-22T12:00:00+08:00` | 2027 | 大雪 | 下元 | true | 是 |
| `2027-12-25T12:00:00+08:00` | 2027 | 大雪 | 下元 | true | 是 |
| `2027-12-26T12:00:00+08:00` | 2027 | 冬至 | 上元 | false | 是 |

9 cases 全部與既有 resolver 的 `qimenSolarTerm` / `yuan` / `isIntercalary` 對齊。2027 年 6 月三個案例 selectedYear 實際為 2026，因為 full cycle draft 是由 2026 大雪 cycle 覆蓋到 2027 年內。2027 年 12 月案例 selectedYear 為 2027。

這代表 `selectedYear` 不等同查詢 civil year，而是實際命中的年度 cycle draft。2027-12-25T12:00 仍落在大雪下元置閏 true，因為奇門有效日尚未進入 2027-12-25T23:00:00+08:00 的冬至上元。2027-12-26T12:00 已落入冬至上元 false。

此結果支持 draft lookup strategy 可複現目前 2027 resolver 初版 timeline entry 層級結果。但尚未比對完整 resolver output，也尚未替換主線。

### 8.9 read-only resolver formatter 草案

第 44 包新增 read-only helper：

```js
resolveQimenJuFromFullTermCycleDraft(dateTimeText, options = {})
```

用途：

- 使用 `findQimenFullTermCycleTimelineDraftEntry(dateTimeText, options)` 取得 draft entry。
- 將 draft entry 轉成接近 `resolveQimenJu(dateTimeText)` 的結果格式。
- 不使用 `INITIAL_QIMEN_TIMELINE`。
- 不修改 `resolveQimenJu()`。
- 不接 UI。
- 不讀取 1080 盤面 JSON。
- 保留 `lookup` metadata 以利觀察 selectedYear / candidateYears。
- 作為未來替換 resolver 主線前的 read-only comparison path。

#### 8.9.1 回傳欄位

formatter 回傳欄位：

```js
{
  actualSolarTerm,
  qimenSolarTerm,
  status,
  yuan,
  dunType,
  dunName,
  ju,
  hourPillar,
  isIntercalary,
  notes,
  lookup
}
```

欄位來源：

- `actualSolarTerm`：沿用既有 actual solar term lookup。
- `qimenSolarTerm` / `yuan` / `isIntercalary`：來自 draft entry。
- `status`：沿用既有 status 判斷邏輯。
- `dunType` / `dunName` / `ju`：沿用 `getQimenYuanJu(...)`。
- `hourPillar`：沿用既有 `getHourPillar(...)` 查詢邏輯。
- `notes`：置閏時回傳 draft formatter 自己的提示文字；非置閏為空陣列。
- `lookup`：保留 `strategy`、`queryEffectiveDayStart`、`selectedYear`、`candidateYears`。

`lookup` 只存在於新 formatter。既有 `resolveQimenJu()` 回傳格式未新增 `lookup` 欄位。formatter 不回傳整個 draft entry 原始物件。formatter 是 read-only 草案，不是正式 resolver replacement。

#### 8.9.2 新增測試

新增測試輸出：

- 奇門完整循環草案resolver formatter測試通過：9 cases

測試內容：

- 7 個 2027 正常案例。
- 2 個錯誤案例：
  - invalid strategy -> `RangeError`
  - missing supported data -> `RangeError`

7 個正常案例與 `resolveQimenJu(...)` 完全比對主要欄位：

- `actualSolarTerm`
- `qimenSolarTerm`
- `status`
- `yuan`
- `dunType`
- `dunName`
- `ju`
- `hourPillar`
- `isIntercalary`

同時驗證：

- `notes` 是陣列。
- 置閏時 `notes.length > 0`。
- 非置閏時 `notes.length = 0`。
- `lookup.strategy = "cycle-year"`。
- `lookup.selectedYear` 是整數。
- `lookup.candidateYears` 是非空陣列。

#### 8.9.3 selectedYear 觀察

| 查詢時間 | selectedYear | 是否與 resolveQimenJu 主要欄位對齊 |
|---|---:|---|
| `2027-06-06T12:00:00+08:00` | 2026 | 是 |
| `2027-06-14T12:00:00+08:00` | 2026 | 是 |
| `2027-12-07T18:00:00+08:00` | 2027 | 是 |
| `2027-12-11T12:00:00+08:00` | 2027 | 是 |
| `2027-12-22T12:00:00+08:00` | 2027 | 是 |
| `2027-12-25T12:00:00+08:00` | 2027 | 是 |
| `2027-12-26T12:00:00+08:00` | 2027 | 是 |

2027 年 6 月案例仍命中 2026 cycle draft。2027 年 12 月案例命中 2027 cycle draft。這再次確認 `selectedYear` 是命中的 cycle draft 年份，不是查詢公曆年。

formatter 目前可在 2027 代表案例上完整對齊既有 resolver 主要欄位。但目前仍只驗證 2027 代表案例，尚未做更大年份範圍比對。

### 8.10 2024～2030 formatter regression 測試

第 46 包新增 2024～2030 `resolveQimenJuFromFullTermCycleDraft(...)` formatter regression 測試，測試輸出：

- 奇門完整循環草案resolver formatter regression測試通過：14 cases

此測試：

- 不與 `resolveQimenJu(...)` 比對。
- 原因是既有 `resolveQimenJu(...)` 目前只覆蓋初版 2027 fixture。
- 測試目標是驗證 draft formatter 在 2024～2030 代表年份可以產生合理 resolver-like result。
- 未修改 `resolveQimenJuFromFullTermCycleDraft(...)`。
- 未修改 `findQimenFullTermCycleTimelineDraftEntry(...)`。
- 未修改 `src/qimenResolver.js`、resolver 主線、UI 或 data JSON。

#### 8.10.1 覆蓋型態

| 類型 | 年份 / 案例 | 說明 |
|---|---|---|
| 有閏大雪 | 2024、2027、2030 | 驗證閏大雪上元與冬至接續 |
| 無置閏 | 2025、2026、2028、2029 | 驗證大雪上元與冬至上元 |
| 年初 fallback | `2028-01-01T12:00:00+08:00` | 查詢 civil year 為 2028，但命中 2027 cycle draft |

#### 8.10.2 14 筆案例摘要

| 查詢時間 | selectedYear | qimenSolarTerm | yuan | isIntercalary | dunName | ju |
|---|---:|---|---|---|---|---:|
| `2024-12-11T12:00:00+08:00` | 2024 | 大雪 | 上元 | true | 陰遁 | 4 |
| `2024-12-26T12:00:00+08:00` | 2024 | 冬至 | 上元 | false | 陽遁 | 1 |
| `2025-12-06T12:00:00+08:00` | 2025 | 大雪 | 上元 | false | 陰遁 | 4 |
| `2025-12-21T12:00:00+08:00` | 2025 | 冬至 | 上元 | false | 陽遁 | 1 |
| `2026-12-01T12:00:00+08:00` | 2026 | 大雪 | 上元 | false | 陰遁 | 4 |
| `2026-12-16T12:00:00+08:00` | 2026 | 冬至 | 上元 | false | 陽遁 | 1 |
| `2027-12-11T12:00:00+08:00` | 2027 | 大雪 | 上元 | true | 陰遁 | 4 |
| `2027-12-26T12:00:00+08:00` | 2027 | 冬至 | 上元 | false | 陽遁 | 1 |
| `2028-01-01T12:00:00+08:00` | 2027 | 冬至 | 中元 | false | 陽遁 | 7 |
| `2028-12-05T12:00:00+08:00` | 2028 | 大雪 | 上元 | false | 陰遁 | 4 |
| `2029-11-30T12:00:00+08:00` | 2029 | 大雪 | 上元 | false | 陰遁 | 4 |
| `2029-12-15T12:00:00+08:00` | 2029 | 冬至 | 上元 | false | 陽遁 | 1 |
| `2030-12-10T12:00:00+08:00` | 2030 | 大雪 | 上元 | true | 陰遁 | 4 |
| `2030-12-25T12:00:00+08:00` | 2030 | 冬至 | 上元 | false | 陽遁 | 1 |

#### 8.10.3 固定統計

| 項目 | 數值 |
|---|---:|
| normalCaseCount | 14 |
| intercalaryCaseCount | 3 |
| nonIntercalaryCaseCount | 11 |
| selectedYearFallbackCount | 1 |
| selectedYearSameAsCivilYearCount | 13 |

3 個置閏案例為 2024、2027、2030 的閏大雪上元。唯一 fallback 案例為 `2028-01-01T12:00:00+08:00`，查詢 civil year 是 2028，但命中 2027 cycle draft。

這再次確認 `selectedYear` 代表命中的 cycle draft 年份，不等於查詢公曆年。2024～2030 regression 通過，表示 formatter 已初步覆蓋有閏、無閏、跨年 fallback 三種情境。

但目前仍不是 1899～2101 full range 驗證，也尚未替換 resolver 主線。

## 9. 目前限制

- full cycle draft 第一版只支援大雪起點。
- `startTerm = "芒種"` 目前明確 throw `RangeError`。
- 此階段尚未判斷任意年份是否都適合以大雪為 cycle 起點。
- 目前較完整的連續觀察範圍是 2024～2030，尚不能推論所有年份。
- 1899～2101 full range diagnostics 已通過，但 `duplicateStarts.length = 69`。
- 尚未把 full cycle timeline draft 接到查詢 resolver。
- 尚未處理任意年份完整覆蓋策略。
- 多年串接目前只完成 2024～2030、2026～2028 與 2027 單年測試。
- 尚未針對 duplicateStarts 設計正式去重策略。
- 尚未確認 duplicate entry 雙方內容是否完全等價或可能存在不同語意。
- duplicateStarts 雖然沒有造成 gap / overlap，但 69 組 duplicate 並非等價。
- 已知第一筆 duplicate 差異為 `qimenSolarTerm`：1909 年 cycle 視為小雪，1910 年 cycle 視為大雪。
- 尚未決定 resolver 查詢時遇到重複 start 應採用哪一筆。
- 尚未判斷遇到 duplicate start 時，應採用前一年度 cycle 還是當年度 cycle。
- 尚未設計以查詢日期選擇年度 cycle 的策略。
- 因此目前仍不可直接替換 `INITIAL_QIMEN_TIMELINE`。
- 因此目前仍不可用現有 deduped timeline 作為正式 resolver timeline。
- 尚未設計多年 timeline 串接時的邊界重疊 / 去重策略。
- 尚未處理跨年度查詢時應載入幾個年度 draft 的策略。
- 尚未決定 resolver 查詢時要載入單年、三年或更大的 timeline window。
- 尚未處理快取策略或效能評估。
- read-only lookup helper 目前只支援 `cycle-year` strategy。
- 尚未驗證所有 duplicate boundary 是否都能由 `cycle-year` 正確處理。
- 已驗證 69 組 duplicate boundary 在 23:30 皆選 current year draft，但尚未把此策略轉成正式 resolver 流程。
- boundary before 有 previous/current year 兩種命中結果，尚未整理其規律。
- 尚未建立完整 duplicate boundary 前後多日的 regression cases。
- draft lookup alignment 目前只驗證 2027 代表案例。
- 目前只比對 timeline entry 層級欄位，尚未比對完整 `resolveQimenJu()` output。
- `selectedYear` 可能與查詢 civil year 不同，例如 2027 年 6 月案例命中 2026 cycle draft。
- 尚未將 draft lookup 結果轉成完整 resolver result。
- 尚未評估以 draft lookup 替換 `INITIAL_QIMEN_TIMELINE` 的完整風險。
- 尚未與 `resolveQimenJu()` 的完整輸出格式整合，例如 `actualSolarTerm`、`status`、`dunType`、`ju`、`hourPillar`。
- 尚未將 lookup result 轉成 `resolveQimenJu()` 的完整格式。
- resolver formatter 目前只在 2027 代表案例上與既有 resolver 主要欄位對齊。
- formatter regression 目前只覆蓋 2024～2030 代表案例。
- 尚未做 1899～2101 full range formatter diagnostics。
- 尚未把 69 組 duplicate boundary 提升到 formatter 層級測試。
- 目前 regression 不與 `resolveQimenJu(...)` 比對，因為主線 resolver 尚未覆蓋 2024～2030 全部年份。
- 尚未驗證 formatter 在所有節氣 / 三元位置上的完整覆蓋。
- 尚未做 1899～2101 full range formatter 比對。
- 尚未驗證所有 duplicate boundary 的 formatter 結果。
- formatter 的 `notes` 文字與既有 resolver 不完全相同，目前只驗證置閏 / 非置閏陣列狀態。
- formatter 額外回傳 `lookup`，不代表正式 UI 應顯示此欄位。
- 尚未決定正式替換 resolver 時是否保留 lookup metadata 或僅內部 debug 使用。
- 尚未處理 lookup helper 的 cache / performance。
- 尚未做效能 / cache 設計。
- 尚未決定是否保留 `candidateYear + 1` fallback。
- 尚未決定是否要讓 UI 或正式 resolver 使用此 helper。
- 尚未實作盤面內容載入。
- 尚未接 UI。
- 尚未做拆補法、茅山法、無閏法或方法切換。

## 10. 後續建議

1. 擴大驗證至資料檔可支援的完整年份範圍，例如 1899～2101 或先分段抽樣。
2. 驗證多年 draft timeline 串接後是否會產生缺口或重疊。
3. 設計 resolver 查詢時的 timeline window 載入策略。
4. 在替換 `INITIAL_QIMEN_TIMELINE` 前，先做 read-only / draft lookup helper。
5. 下一步可做 1899～2101 全範圍 diagnostics 掃描測試或先做抽樣。
6. 可新增 read-only lookup helper，從多年 draft timeline 中查找某一時間點，但仍不替換 `resolveQimenJu()`。
7. 等 full range diagnostics 穩定後，再討論是否替換 `INITIAL_QIMEN_TIMELINE`。
8. 檢查 69 筆 duplicateStarts 的內容差異，確認是否只是相鄰年度 full cycle 重疊。
9. 新增 duplicate detail diagnostics，記錄重複 entry 的 `qimenSolarTerm` / `yuan` / `isIntercalary` / `sourceDayPillar` 是否一致。
10. 設計正式 timeline 去重規則。
11. 再做 read-only lookup helper，避免尚未處理 duplicate 前直接接 resolver。
12. 先針對 duplicate start 設計年度 cycle 選擇策略。
13. 比較「以查詢時間所在 civil year 選年度 draft」與「以大雪 cycle year 選年度 draft」兩種策略。
14. 新增 read-only lookup helper 時，應避免全域合併後直接 dedupe，而應先決定查詢時間要使用哪一個 yearDraft。
15. 針對 duplicateStart 前後日期建立查詢案例，例如 1910-11-24、1910-11-25、2027 既有案例，確認應落在哪個 cycle。
16. 暫不設計正式去重規則，先做 lookup strategy 實驗。
17. 針對 69 組 duplicate boundary 自動產生 lookup boundary 測試，確認 cycle-year strategy 的行為是否一致。
18. 新增 helper 將 draft entry 轉成接近 `resolveQimenJu()` 的完整結果，但仍不替換主線。
19. 比對 2027 既有 resolver 初版結果與 draft lookup 結果是否一致。
20. 評估 candidate year fallback 順序是否足夠，尤其是資料邊界 1899 / 2101。
21. 在大量查詢前設計 yearDraft cache。
22. 針對 69 組 duplicate boundary 的 boundary before 23 / 46 分布做後續分析，找出是否與年度 cycle 起點或置閏狀態有關。
23. 在替換 resolver 前，先建立更完整的 cross-year lookup regression cases。
24. 評估是否需要保留 candidateYear + 1 fallback。
25. 新增 draft lookup result formatter，將 draft entry 轉成接近 `resolveQimenJu()` 的 read-only 結果。
26. 建立 formatter 測試，比對 2027 既有 resolver output 的 `actualSolarTerm`、`status`、`dunType`、`dunName`、`ju`、`hourPillar`、`isIntercalary`。
27. 針對 2027 以外年份新增 read-only draft lookup regression cases。
28. 明確記錄 `selectedYear` 是命中的 cycle draft 年份，不等於查詢 civil year。
29. 新增 full range read-only formatter comparison diagnostics，抽樣或全範圍比對 draft formatter 與既有 resolver 可覆蓋範圍。
30. 先針對 2024～2030 做 formatter regression cases。
31. 針對 69 組 duplicate boundary 的 boundary after / before 加入 formatter 層級 regression。
32. 設計 formatter / lookup cache，避免大量查詢時重複 build yearDraft。
33. 討論正式切換時是否要讓 `resolveQimenJu()` 內部改用 draft formatter，或保留兩條路徑一段時間。
34. 討論 `lookup` metadata 是否只在 debug/dev mode 開啟。
35. 針對 69 組 duplicate boundary 新增 formatter 層級 regression。
36. 新增 2024～2030 更多節氣 / 三元位置的 formatter cases。
37. 規劃 1899～2101 full range formatter diagnostics，但先避免直接把大量結果輸出到 console。
38. 評估是否先做 yearDraft cache，再做 full range formatter diagnostics。
39. 討論是否以大雪 / 冬至附近作為年度完整 cycle anchor。
40. 替換前先保留草案 helper，不直接接 resolver。
41. 等年度完整 timeline 穩定後，再考慮盤面 JSON lookup 與 UI 顯示。

## 11. 本階段結論

第 25～26 包已完成年度 full cycle 草案 pipeline。目前程式主線仍安全不變。此階段可作為後續完整年度奇門 timeline 的基礎。

第 28 包已完成 2026 / 2028 跨年份草案驗證。目前大雪起點策略在 2026～2028 三年可產出符合測試預期的 full cycle timeline 草案。後續仍需更多年份驗證或 anchor 策略設計後，才能考慮替換 resolver 主線。

第 30 包已完成 2024～2030 多年觀察測試。目前 2024～2030 皆可由大雪起點產生 full cycle draft timeline。2024 / 2027 / 2030 為閏大雪，timeline 75 筆；2025 / 2026 / 2028 / 2029 無置閏，timeline 72 筆。仍須更多年份驗證後，才能討論是否替換 resolver 主線。

第 32 包完成多年 full cycle draft timeline 串接 helper。2024～2030 串接 diagnostics 目前為 duplicateStarts = 0、gaps = 0、overlaps = 0。此結果支持後續進一步做 full range diagnostics 與 read-only lookup helper。目前仍未替換 resolver 主線。

第 34 包已完成 1899～2101 full range diagnostics 掃描。結果沒有 gap / overlap，但有 69 個 duplicate start。full range 年度統計顯示 203 年中 94 年有置閏、109 年無置閏，且沒有任何年份出現多重置閏。下一步應針對 duplicate start 做 detail diagnostics，而不是直接替換 resolver 主線。

第 36 包確認 full range duplicateStarts 不是完全等價。69 組 duplicate 全部來自相鄰年度，但 `qimenSolarTerm` 可能不同。因此下一階段重點應由「單純去重」改為「查詢時選擇正確年度 cycle / timeline window」。在完成 lookup strategy 前，不應替換 `INITIAL_QIMEN_TIMELINE` 或接 `resolveQimenJu()`。

第 38 包完成 read-only lookup strategy 草案 helper。cycle-year strategy 可以處理 1910 duplicate boundary 與 2028 年初跨 cycle fallback。此結果支持後續從「全域 timeline dedupe」轉向「查詢時選擇年度 draft / cycle window」。目前仍未替換 `INITIAL_QIMEN_TIMELINE`，也未接 `resolveQimenJu()`。

第 40 包完成 69 組 duplicate boundary 自動化 lookup 測試。結果顯示 boundary after 全部穩定選 current year draft，boundary before 則只落在 previous/current year。這進一步支持以 lookup strategy 選年度 cycle，而不是全域 dedupe。目前仍未替換 `INITIAL_QIMEN_TIMELINE`，也未接 `resolveQimenJu()`。

第 42 包完成 2027 resolver 初版結果 vs draft lookup entry 對齊測試。9 個 2027 代表案例在 `qimenSolarTerm` / `yuan` / `isIntercalary` 層級全部對齊。2027 年 6 月案例命中 2026 cycle draft，說明未來文件與 UI 若顯示 selectedYear，必須避免誤解為查詢公曆年。目前仍未替換 `INITIAL_QIMEN_TIMELINE`，也未接 `resolveQimenJu()` 主線。

第 44 包完成 read-only resolver formatter 草案。新 helper 已可把 full cycle draft lookup entry 轉成接近 `resolveQimenJu()` 的結果格式。7 個 2027 正常案例與既有 resolver 主要欄位全部對齊，2 個錯誤案例皆固定為 `RangeError`。目前仍未替換 `INITIAL_QIMEN_TIMELINE`，也未改 `resolveQimenJu()` 主流程。下一階段可進入更大範圍 formatter comparison diagnostics 或先補 2024～2030 regression。

第 46 包完成 2024～2030 formatter regression 測試。14 個案例覆蓋有閏大雪、無置閏與年初 fallback。統計固定為置閏 3、非置閏 11、selectedYear fallback 1。這表示 read-only formatter 已不只適用於 2027 初版代表案例，也能在 2024～2030 代表年份穩定產生 resolver-like result。目前仍未替換 `INITIAL_QIMEN_TIMELINE`，也未改 `resolveQimenJu()` 主流程。

完成本文件補充後，第 46～47 包可一起 commit。
