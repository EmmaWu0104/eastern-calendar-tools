# 奇門遁甲正式盤面 UI 渲染驗收報告

## 1. 本包目的

本文件是新第 118 包 docs-only 驗收報告，目的為整理第 117 包奇門正式盤面 UI 渲染完成狀態與驗收結果。

本包只整理第 117 UI 渲染驗收報告：

* 不修改 code。
* 不修改 tests。
* 不修改 data。
* 不產生 JSON。

## 2. UI 渲染完成摘要

第 117 包完成狀態：

* `getQimenPlate` 正式資料已接入 UI。
* placeholder 已替換為正式盤面。
* 3x3 九宮正式渲染完成。
* 「待接入」已不存在。
* 董公擇日仍 `hidden`。

## 3. 顯示內容驗收

顯示內容驗收結果：

| item | result |
|---|---|
| 9 宮 | 存在 |
| 宮位順序 | 巽、離、坤 / 震、中、兌 / 艮、坎、乾 |
| 每宮欄位 | 天 / 地 / 星 / 門 / 神 |
| null 欄位 | 顯示為 dash 或空白 fallback，不顯示 `null` 字樣 |
| 中宮 | 顯示天禽 |
| 直符 badge | 可見 |
| 直使 badge | 可見 |
| 天禽寄宮 | 未推導，仍照資料標記顯示 |

## 4. 手動覆寫驗收

手動覆寫驗收結果：

* 手動覆寫仍只影響盤面查表。
* 自動定局摘要仍維持。
* 手動覆寫遁局時盤面會變。
* 未修改手動覆寫邏輯。

## 5. 安全邊界

第 117 包安全邊界：

* 未修改 `data/1080.md`。
* 未修改 `data/qimen/plates/**`。
* 未修改 `data/qimen/qimen_yuan_ju_table.json`。
* 未修改 `getQimenPlate` implementation。
* 未修改 resolver。
* 未修改董公擇日顯示狀態。
* 未做天禽寄宮推導。
* 未做空亡 / 驛馬推導。
* 未做格局、卦名、斷語、吉凶。

## 6. 驗證結果

第 117 包驗證結果：

| check | result |
|---|---|
| `npm test` | 通過 |
| `node --check src/main.js` | 通過 |
| `git diff --check` | 通過 |
| dev server / manual preview | 通過 |
| Edge CDP DOM 檢查 | 通過 |
| server cleanup | 已關閉 server，無長駐程序殘留 |

Edge CDP DOM 檢查確認：

* 「待接入」不存在。
* `.qimen-palace-cell` 數量為 9。
* 天 / 地 / 星 / 門 / 神 label 存在。
* 中宮包含天禽。
* 直符 / 直使 badge 可見。
* 董公擇日 section 仍為 `hidden`。

## 7. git 狀態

git 狀態記錄：

* 第 117 無新 code diff 可 commit。
* 目前 `git status` 只剩 unrelated untracked files：
  * `2Drive.ffs_gui`
  * `玄學小工具.txt`
* 本包新增 `docs/72_奇門遁甲正式盤面UI渲染驗收報告.md` 後，`git status` 預期只多出該 docs file。

## 8. 目前限制

目前限制：

* 天禽寄宮未推導。
* `xunShou` 仍為 null，UI 暫不顯示。
* `isEmpty` / 空亡未推導。
* `isHorse` / 驛馬未推導。
* 格局、卦名、斷語、吉凶未做。
* method switch 未做，仍只做傳統置閏法主線。
* `source.raw` 不顯示在正式 UI。

## 9. 後續建議

後續建議：

* 下一包可做 CSS / 手機版微調，若畫面密度需要。
* 或做奇門正式盤面 UI 階段交接摘要 docs-only。
* 不建議立刻加入格局斷語或新推導。
* 若要做天禽寄宮、空亡、驛馬，應另開資料規則規劃包。

## 10. 本包不處理項目

本包不處理：

* 不修改 code。
* 不修改 tests。
* 不修改 data。
* 不產生 JSON。
* 不修改 `getQimenPlate`。
* 不修改 resolver。
* 不接新資料。
* 不做新推導。
* 不 commit。

## 11. 建議 commit message

```text
docs: verify qimen formal plate UI render
```
