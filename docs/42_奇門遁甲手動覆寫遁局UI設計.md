# 42 奇門遁甲手動覆寫遁局 UI 設計

本文件是第 75 包 docs-only 設計文件，目標是設計未來奇門 UI 的手動覆寫遁別 / 局數功能。

本包不實作 UI、不修改 resolver、不修改 plate lookup helper、不修改 data JSON。手動覆寫只影響未來 plate lookup effective input，不回寫 formatter result，也不代表拆補法、茅山法、無閏法或 method switch。本專案第一版仍只做傳統置閏法。

## 1. 需求背景

目前奇門 UI 第一版已完成：

* 顯示 full cycle draft formatter 的自動定局結果。
* 顯示缺盤 fallback。
* layout 已調整為左側定局摘要、右側盤面區。

未來若要查盤，有時使用者可能希望人工覆寫：

* 遁別：陽遁 / 陰遁
* 局數：一局～九局

但這個覆寫用途只限於：

* 未來盤面 lookup 使用的 effective dunType / ju。
* 協助人工校對或特殊情境查表。
* 不改變自動定局結果。
* 不改變奇門置閏法 resolver。

## 2. 核心設計原則

### 2.1 自動結果永遠保留

formatter result 是自動定局結果：

* `autoDunType`
* `autoDunName`
* `autoJu`
* `hourPillar`
* `qimenSolarTerm`
* `yuan`
* `status`
* `isIntercalary`
* `notes`

手動覆寫不得修改 formatter result。

### 2.2 手動覆寫只影響 plate lookup

若使用者啟用手動覆寫：

* plate lookup 使用 manual `dunType`
* plate lookup 使用 manual `ju`
* `hourPillar` 仍使用 formatter result
* qimen summary 仍顯示自動定局
* 可另外顯示「盤面查表使用：陰遁九局」

### 2.3 不是 method switch

手動覆寫：

* 不是拆補法。
* 不是茅山法。
* 不是無閏法。
* 不是不同奇門派別切換。
* 不是 resolver replacement。
* 不是 `resolveQimenJu()` 主線切換。
* 只是 UI / plate lookup 層的查表覆寫。

### 2.4 可關閉、可還原

手動覆寫應有明確開關：

* 預設關閉。
* 關閉時使用自動定局。
* 啟用時才使用 manual effective value。
* 可一鍵恢復自動。

## 3. 建議資料模型

資料結構：

```js
{
  auto: {
    dunType: "yang",
    dunName: "陽遁",
    ju: 1,
    hourPillar: "丙午"
  },
  manual: {
    enabled: false,
    dunType: "yang",
    dunName: "陽遁",
    ju: 1
  },
  effectiveForPlateLookup: {
    dunType: "yang",
    dunName: "陽遁",
    ju: 1,
    source: "auto"
  }
}
```

啟用手動覆寫後：

```js
{
  auto: {
    dunType: "yang",
    dunName: "陽遁",
    ju: 1,
    hourPillar: "丙午"
  },
  manual: {
    enabled: true,
    dunType: "yin",
    dunName: "陰遁",
    ju: 9
  },
  effectiveForPlateLookup: {
    dunType: "yin",
    dunName: "陰遁",
    ju: 9,
    source: "manual"
  }
}
```

補充：

* `hourPillar` 不進 manual。
* `hourPillar` 永遠來自 formatter。
* `effectiveForPlateLookup` 是給 `getQimenPlate(...)` 用。
* 自動 summary 不應被 manual 覆蓋。

## 4. UI 顯示設計

### 4.1 預設狀態

預設不啟用手動覆寫。

左側定局摘要：

```text
奇門節氣：冬至
元別：上元
遁別：陽遁
局數：一局
時柱：丙午
狀態：正常
```

右側盤面區：

```text
盤面
目前使用：自動定局（陽遁一局）
盤面資料尚未建立，目前僅顯示定局結果。
```

### 4.2 啟用手動覆寫

右側盤面區可顯示：

```text
盤面
自動定局：陽遁一局
盤面查表使用：陰遁九局（手動）
盤面資料尚未建立，目前僅顯示定局結果。
```

左側定局摘要仍顯示原本自動結果，不被覆寫。

### 4.3 控制元件

建議控制元件：

* checkbox / switch：`手動覆寫盤面遁局`
* select：`遁別`
  * 陽遁
  * 陰遁
* select：`局數`
  * 一局～九局
* button / link：`恢復自動`

第一版可以放在右側盤面區，而不是左側定局摘要。

### 4.4 狀態提示

啟用 manual 時，顯示提示：

```text
手動覆寫只影響盤面查表，不改變左側自動定局。
```

## 5. plate lookup 整合流程

目前流程：

```text
qimen result
-> getQimenPlate({
     dunType: qimen.dunType,
     ju: qimen.ju,
     hourPillar: qimen.hourPillar
   })
```

未來有 manual override 後：

```text
qimen result
-> resolve effective plate input
-> getQimenPlate({
     dunType: effective.dunType,
     ju: effective.ju,
     hourPillar: qimen.hourPillar
   })
```

建議 helper：

```js
function resolveQimenPlateLookupInput(qimen, manualOverride) {
  // normalize manual state and return effective lookup input
}
```

回傳：

```js
{
  dunType: "yin",
  dunName: "陰遁",
  ju: 9,
  hourPillar: "丙午",
  source: "manual"
}
```

若未啟用 manual：

```js
{
  dunType: qimen.dunType,
  dunName: qimen.dunName,
  ju: qimen.ju,
  hourPillar: qimen.hourPillar,
  source: "auto"
}
```

## 6. 狀態管理建議

依目前 `src/main.js` 風格，可新增 module-level 狀態：

```js
let qimenManualOverride = { enabled: false, dunType: "", ju: null };
```

當查詢時間變更時有兩種策略：

| 策略 | 行為 | 優點 | 風險 |
| -- | -- | -- | -- |
| 保留 manual enabled | 查詢時間變更後仍保留 manual 選擇 | 適合連續校對同一組覆寫盤 | 使用者可能忘記目前正在手動覆寫 |
| 每次查詢重置 manual | 查詢時間變更後回到自動 | 最不容易誤用 | 連續校對時較麻煩 |

建議第一版：

* 查詢時間變更時保留 manual enabled，但 manual select 預設同步最新自動值。
* 若使用者未手動啟用，就永遠跟著自動值。
* 若已啟用 manual，保留 manual 選擇。

提醒：

* 不要讓 manual 狀態污染 formatter result。
* 不要把 manual 狀態寫入 URL 或 localStorage，第一版不需要持久化。

## 7. 錯誤與 fallback

錯誤規則：

* manual dunType 不合法：回到 auto 或顯示 invalidInput。
* manual ju 不合法：回到 auto 或顯示 invalidInput。
* 建議第一版在 UI 控制元件限制合法值，所以不應出現非法 manual。
* 若 effective input 仍造成 `getQimenPlate(...)` 回 `invalidInput`：
  * 顯示 `奇門盤面查詢參數不完整，暫時無法顯示盤面。`
  * 左側定局摘要仍保留。

## 8. 未來實作測試建議

若第 76 包實作手動覆寫，建議測：

### 8.1 default auto mode

* 查詢 `2027-12-26T12:00`
* 自動結果陽遁一局
* effective source = auto
* getQimenPlate 使用 yang / 1 / hourPillar

### 8.2 manual enabled

* 查詢 `2027-12-26T12:00`
* 啟用 manual：陰遁九局
* 左側仍顯示陽遁一局
* 右側顯示盤面查表使用陰遁九局
* getQimenPlate 使用 yin / 9 / same hourPillar

### 8.3 restore auto

* 啟用 manual 後按恢復自動
* effective source = auto
* 右側回到自動定局

### 8.4 query time changed

* 時間變更後自動定局更新
* manual UI 狀態符合設計策略
* 不污染 formatter result

### 8.5 no debug leakage

* 不顯示 lookup metadata
* 不顯示 cache stats
* 不顯示 raw JSON

## 9. 不做項目

* 不在本設計中實作真實九宮盤面。
* 不在本設計中修改 plate JSON。
* 不在本設計中新增 method switch。
* 不在本設計中新增拆補法、茅山法、無閏法。
* 不在本設計中修改 resolver。
* 不在本設計中修改 formatter。
* 不在本設計中持久化使用者 manual 選擇。
* 不在本設計中顯示 cache stats。
* 不在本設計中顯示 lookup metadata debug panel。

## 10. 建議後續實作分包

建議：

* 第 76 包：實作手動覆寫 UI 控制元件與 effective plate lookup input。
* 第 77 包：docs 補手動覆寫實作結果。
* 第 78 包：盤面 object schema 設計。
* 第 79 包以後：真實 plate data / rendering。

也可以選擇：

* 暫停手動覆寫，先做奇門 UI 第一版人工驗收。
* 暫停奇門 UI，回到資料整理。

## 11. 本文件結論

手動覆寫是 UI / plate lookup 層功能。手動覆寫只改變 `getQimenPlate(...)` 的 effective dunType / ju，不修改 formatter result，也不代表 method switch。

左側自動定局摘要應永遠保留。右側盤面區可顯示自動 vs 手動查表使用值。下一步若要實作，應保持小包，只做控制元件與 effective lookup input，不做真實盤面。

## 12. 第 76 包實作結果

第 76 包已完成手動覆寫 UI 第一版：

* 修改 `src/main.js`
* 修改 `styles/main.css`
* 新增 module-level 狀態：
  * `qimenManualOverride`
* 新增右側盤面區手動覆寫控制
* 新增 checkbox：
  * `手動覆寫盤面遁局`
* 新增 select：
  * 遁別：陽遁 / 陰遁
  * 局數：一局～九局
* 新增 button：
  * `恢復自動`
* 新增 manual hint：
  * `手動覆寫只影響盤面查表，不改變左側自動定局。`
* 新增 effective lookup input 流程
* `getQimenPlate(...)` 改用 effective dunType / ju / hourPillar 查詢
* 左側自動定局摘要仍使用 formatter result，不受手動覆寫影響
* 未修改 resolver
* 未修改 plate lookup helper
* 未修改 data JSON
* 未做真實九宮盤面
* 未顯示 lookup metadata / cache stats

### 12.1 code change summary

`src/main.js` 第 76 包新增 / 調整：

* 新增 `qimenManualOverride`
* 新增 event listeners：
  * `manualToggle.change`
  * `manualDunSelect.change`
  * `manualJuSelect.change`
  * `manualRestore.click`
* `createQimenSection()` 新增：
  * manual toggle
  * manual dun select
  * manual ju select
  * restore auto button
  * manual hint
* 新增 / 調整 helper：
  * `handleQimenManualToggleChange()`
  * `handleQimenManualDunChange()`
  * `handleQimenManualJuChange()`
  * `restoreQimenAutoPlateLookup()`
  * `rerenderCurrentQimenSection()`
  * `syncQimenManualControlsWithAuto(qimen)`
  * `renderQimenManualControlState()`
  * `resolveQimenPlateLookupInput(qimen, manualOverride)`
  * `isValidQimenManualOverride(manualOverride)`
  * `renderQimenPlateUsage(qimen, effective)`
  * `createQimenPlateUsageLine(text)`
  * `createQimenManualField(labelText)`
  * `createOption(value, text)`
  * `getQimenDunName(dunType)`
* `renderQimenSection(dateTimeText)` 流程改為：
  * formatter 取得 qimen
  * sync manual controls
  * resolve effective plate lookup input
  * 左側 summary 仍 render qimen 自動結果
  * 右側 usage 顯示 auto 或 manual
  * `getQimenPlate(...)` 使用 effective input

`styles/main.css` 第 76 包新增 / 調整：

* 新增 qimen plate usage 樣式
* 新增 qimen manual controls 樣式
* 新增 qimen manual toggle / checkbox 樣式
* 新增 qimen manual fields / select / restore button 樣式
* 新增 qimen manual hint 樣式

## 13. 第 76 包驗證結果

* `npm test`：通過
* `git diff --check`：通過
* `node --check src/main.js`：通過
* `node --check tests/run-tests.js`：未執行，因為未修改 tests

Smoke：

| 測試 | 結果 |
|---|---|
| default auto | 通過，陽遁一局、select disabled、顯示自動定局與缺盤 fallback |
| manual enabled | 通過，左側仍陽遁一局，右側顯示陰遁九局（手動） |
| restore auto | 通過，回到陽遁一局自動定局 |
| 置閏案例 | 通過，自動同步陰遁四局並顯示缺盤 fallback |

補充：

* 左側自動定局不受 manual 影響。
* 右側 plate lookup 使用 effective manual input。
* restore auto 正常。
* 未顯示 lookup metadata / cache stats。
* 未做九宮盤面。

## 14. 第 76A 包 layout 微調結果

使用者畫面回饋：

* 第 76 包功能正確，但 layout 中 checkbox 與文字佔用中段大面積。
* checkbox 因全站 `input { min-height: 40px; }` 影響而過大。
* 使用者希望「手動覆寫盤面遁局」移到右側白底盤面區上方。

第 76A 包調整：

* 將 checkbox 移到右側 plate panel 標題列。
* checkbox 與 `盤面` 標題同列。
* manual fields 只保留：
  * 遁別
  * 局數
  * 恢復自動
  * hint
* checkbox 固定為 16x16。
* 手動覆寫邏輯未變。
* resolver / plate lookup / data JSON 未修改。

驗證：

* `npm test`：通過
* `git diff --check`：通過
* `node --check src/main.js`：通過
* checkbox 尺寸正常
* 手動覆寫與恢復自動仍正常
* 左側自動定局摘要不受手動覆寫影響

## 15. 第 76B 包 layout 微調結果

使用者第二次畫面回饋：

* 第 76A 包方向正確，但操作順序還可以更直覺。
* 使用者希望：
  1. 先勾選手動覆寫
  2. 旁邊直接改遁別 / 局數
  3. 下方看到目前使用 / 自動定局資訊
  4. 再下方顯示盤面 / fallback

第 76B 包調整：

* 將 manual controls 移到右側 plate panel 最上方。
* 新增 `qimen-manual-control-row`。
* `manualToggleLabel` 與 `manualFields` 放同一列。
* 遁別 / 局數 / 恢復自動移到 checkbox 旁邊。
* `盤面` 標題改到 controls 下方。
* `plateUsage` 在 `盤面` 標題下方。
* fallback 在最下方。
* 窄版時 manual control row 改成單欄。

目前視覺順序：

```text
[ ] 手動覆寫盤面遁局   遁別   局數   恢復自動
盤面
目前使用：自動定局（陰遁八局）
盤面資料尚未建立，目前僅顯示定局結果。
```

manual enabled 時：

```text
[x] 手動覆寫盤面遁局   遁別   局數   恢復自動
盤面
自動定局：陽遁一局
盤面查表使用：陰遁九局（手動）
盤面資料尚未建立，目前僅顯示定局結果。
```

驗證：

* `npm test`：通過
* `git diff --check`：通過
* `node --check src/main.js`：通過
* checkbox 已移到右側 plate panel 左上方
* 遁別 / 局數 / 恢復自動已移到 checkbox 旁邊
* checkbox 維持 16x16
* 左側自動定局仍不受手動覆寫影響
* manual enabled / restore auto 正常
* 未顯示 lookup metadata / cache stats
* 未做九宮盤面

## 16. 目前手動覆寫 UI 狀態

* 右側盤面區最上方為 manual control row。
* 預設未勾選。
* 未勾選時：
  * select disabled
  * restore auto disabled
  * plate lookup 使用 auto result
  * 顯示 `目前使用：自動定局（...）`
* 勾選後：
  * select enabled
  * restore auto enabled
  * plate lookup 使用 manual effective input
  * 左側 summary 仍顯示自動結果
  * 右側顯示：
    * `自動定局：...`
    * `盤面查表使用：...（手動）`
* restore auto：
  * 關閉 manual
  * select 同步目前自動定局
  * plate lookup 回到 auto result
* 查詢時間變更：
  * 若 manual disabled，select 同步最新自動值
  * 若 manual enabled，保留 manual 選擇
* 目前仍沒有：
  * 真實九宮盤面
  * lookup metadata
  * cache stats
  * method switch
  * data persistence

## 17. 目前限制與後續建議

目前限制：

* 手動覆寫目前只影響缺盤 fallback 查詢的 effective input。
* 因 1080 plate JSON 仍是 null skeleton，所以 manual 後仍顯示缺盤 fallback。
* 尚未顯示真實盤面差異。
* 尚未有盤面 object schema。
* 尚未做九宮盤面 rendering。
* 尚未持久化 manual 選擇。
* 尚未加入 debug panel。

後續建議：

1. 先 commit 第 76～77 包。
2. 若目前手動覆寫 UI OK，可暫停 UI 功能擴張。
3. 下一階段若要繼續，建議先做：
   * 盤面 object schema 設計
   * 1080 plate data 填寫策略
   * 盤面 rendering 設計
4. 不建議下一包直接填 plate JSON。
5. 不建議下一包直接做完整九宮盤面。
6. 不建議把 method switch 與手動覆寫混在一起。

## 18. 第 76～77 包結論

* 第 76 包完成手動覆寫盤面遁局 UI 第一版。
* 手動覆寫只影響 `getQimenPlate(...)` 的 effective dunType / ju。
* 左側自動定局摘要不受手動覆寫影響。
* 第 76A / 76B 包依使用者截圖回饋完成 layout 微調。
* 目前操作順序已調整為：
  * 先勾選手動覆寫
  * 旁邊調整遁別 / 局數
  * 下方看目前使用與盤面 fallback
* 目前仍未做真實九宮盤面與 plate data。
* 本階段可作為手動覆寫 UI 第一版 checkpoint。
