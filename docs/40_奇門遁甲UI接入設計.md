# 40 奇門遁甲 UI 接入設計

本文件是第 71 包 docs-only UI 接入設計，目標是為第 72 包前端實作做準備。第 72 包預計只做「奇門定局摘要 + 缺盤 fallback」。

本包不實作 UI、不修改 formatter / resolver / plate lookup、不修改 1080 plate JSON、不加入手動覆寫，也不加入拆補法、茅山法、無閏法或 method switch。

## 1. 目前可用能力

### 1.1 定局 formatter

可使用：

```js
resolveQimenJuFromFullTermCycleDraft(dateTimeText)
```

目前提供：

* `actualSolarTerm`
* `qimenSolarTerm`
* `status`
* `yuan`
* `dunType`
* `dunName`
* `ju`
* `hourPillar`
* `isIntercalary`
* `notes`
* `lookup`

這是後續 UI 建議使用的奇門定局來源。它已改用 cache，並已通過 regression / duplicate boundary / full range diagnostics。UI 第一版不使用 `resolveQimenJu()` 主線。

### 1.2 plate lookup

可使用：

```js
getQimenPlate({
  dunType,
  ju,
  hourPillar,
})
```

目前提供：

* `found`
* `status`
* `message`
* `meta`
* `plate`
* `lookup`

目前 plates 是 skeleton / null，大多會回 `status: "nullPlate"`。這不是錯誤，UI 應顯示定局摘要 + 缺盤 fallback。

## 2. 第 72 包 UI 接入範圍

第 72 包只做：

* 在頁面新增 / 顯示「奇門遁甲」區塊。
* 呼叫 `resolveQimenJuFromFullTermCycleDraft(...)` 取得定局結果。
* 呼叫 `getQimenPlate(...)` 取得盤面 lookup 結果。
* 顯示奇門定局摘要。
* 若 plate 為 `nullPlate` / `missingPlate` / `missingFile`，顯示缺盤 fallback。
* 若 formatter 查詢失敗，顯示奇門資料無法查詢 fallback。
* 不顯示完整九宮盤面。
* 不做手動覆寫。
* 不做 debug panel。
* 不修改 plate JSON。
* 不修改 resolver / formatter。

明確不做：

* 不做真實 1080 盤面渲染。
* 不做宮位 / 門 / 星 / 神顯示。
* 不做手動覆寫遁別 / 局數。
* 不做 method switch。
* 不做 `resolveQimenJu()` replacement。
* 不做 cache stats 顯示。
* 不做 lookup metadata 預設顯示。

## 3. 建議 UI 區塊位置

根據目前頁面既有玄學區塊設計，建議：

* 奇門遁甲區塊放在金函玉鏡日盤後面，或放在九宮飛星 / 金函玉鏡附近。
* 若目前頁面已有多個 card / section，請用同樣風格新增一個 card。
* 標題建議：`奇門遁甲`
* 副標 / 小字可選：`置閏法定局`
* 第一版不需要九宮格盤面，只顯示定局摘要與 fallback。

提醒：

* 不要把奇門區塊混進金函玉鏡區塊。
* 不要共用金函玉鏡的「陽遁 / 陰遁」判斷文字，以免語意混淆。
* 奇門的遁局來自 `resolveQimenJuFromFullTermCycleDraft(...)`。

## 4. UI 資料流程

建議流程：

```text
目前查詢時間 / dateTimeText
-> resolveQimenJuFromFullTermCycleDraft(dateTimeText)
-> qimen result: dunType / dunName / ju / hourPillar / qimenSolarTerm / yuan / status / notes
-> getQimenPlate({ dunType, ju, hourPillar })
-> plate result
-> render 定局摘要
-> 若 plate.status !== "found"，render fallback message
```

formatter error 要 catch。plate lookup 不應影響 formatter 定局結果。plate lookup 的 fallback message 可直接使用 `plate.message`。

若 `plate.message` 為空但 `plate.status !== "found"`，UI 可使用預設：

```text
盤面資料尚未建立，目前僅顯示定局結果。
```

## 5. 建議顯示內容

### 5.1 正常定局摘要

第一版建議顯示：

```text
奇門遁甲
置閏法定局

奇門節氣：冬至
元別：上元
遁別：陽遁
局數：一局
時柱：丙午
狀態：正常

盤面資料尚未建立，目前僅顯示定局結果。
```

### 5.2 置閏案例

```text
奇門遁甲
置閏法定局

奇門節氣：大雪（置閏）
元別：上元
遁別：陰遁
局數：四局
時柱：丙午
狀態：置閏
備註：查詢時間落在 full cycle draft 置閏 timeline 內。

盤面資料尚未建立，目前僅顯示定局結果。
```

### 5.3 實際節氣與奇門節氣不同

當 `actualSolarTerm !== qimenSolarTerm` 時，建議顯示：

```text
實際節氣：冬至
奇門節氣：大雪（置閏）
狀態：置閏後接氣
```

若相同，第一版可不顯示實際節氣。若不同，顯示實際節氣有助理解超神 / 接氣 / 置閏後接氣。

### 5.4 formatter 查詢失敗

若 formatter throw，顯示：

```text
奇門遁甲資料目前無法查詢此時間。
```

不要讓整個頁面壞掉。

## 6. 欄位格式化規則

### 6.1 局數格式

`ju` 可以先轉中文：

| ju | 顯示 |
| -- | -- |
| 1 | 一局 |
| 2 | 二局 |
| 3 | 三局 |
| 4 | 四局 |
| 5 | 五局 |
| 6 | 六局 |
| 7 | 七局 |
| 8 | 八局 |
| 9 | 九局 |

若轉換失敗，可 fallback：

```text
{ju}局
```

### 6.2 置閏顯示

若 `isIntercalary === true`：

```text
奇門節氣：大雪（置閏）
```

若 false：

```text
奇門節氣：冬至
```

### 6.3 notes

若 `Array.isArray(notes) && notes.length > 0`：

* 顯示備註行。
* 多筆 notes 可用 `；` 串接。

若 notes 空：

* 不顯示備註。

### 6.4 status

status 直接顯示原字串：

* 正常
* 超神
* 接氣
* 置閏
* 置閏後接氣

不要在 UI 層重新判斷 status。

## 7. 錯誤與 fallback 規則

| 狀況 | UI 行為 |
| -- | -- |
| formatter 成功、plate `found` | 顯示定局摘要；盤面區留待未來渲染 |
| formatter 成功、plate `nullPlate` | 顯示定局摘要 + 缺盤 fallback |
| formatter 成功、plate `missingPlate` | 顯示定局摘要 + 缺盤 fallback |
| formatter 成功、plate `missingFile` | 顯示定局摘要 + 缺盤 fallback |
| formatter 成功、plate `invalidInput` | 顯示定局摘要 + 盤面查詢參數錯誤 fallback |
| formatter throw | 顯示奇門資料無法查詢 fallback；不要呼叫 plate lookup |
| plate lookup throw（理論上不應） | 顯示定局摘要 + 盤面資料讀取失敗 fallback |

第 72 包不需要建立 debug panel。console.error 是否保留由實作決定，但不要在 UI 顯示技術細節。

## 8. CSS / layout 建議

第 72 包建議：

* 沿用現有 card / section 樣式。
* 使用簡單 rows / grid 顯示欄位。
* 避免新增複雜九宮盤樣式。
* fallback message 可使用較淡的提示樣式。
* 置閏可用小 badge，但第一版可先純文字。
* 行動版不要過度擠壓。
* 不要大幅調整既有區塊 spacing。

可建議 class naming：

```text
qimen-section
qimen-summary
qimen-summary-row
qimen-label
qimen-value
qimen-note
qimen-fallback
qimen-badge
```

實際 class naming 應依現有 CSS 風格調整，不要大規模重構 CSS。

## 9. 第 72 包測試建議

若第 72 包接 UI，建議至少測：

### 9.1 JS syntax

* `node --check src/main.js`

### 9.2 unit / regression

* `npm test`

### 9.3 smoke cases

用瀏覽器或 Playwright / jsdom 如既有可行，檢查：

1. 一般冬至案例：
   * `2027-12-26T12:00:00+08:00`
   * 顯示冬至 / 上元 / 陽遁 / 一局
   * 顯示缺盤 fallback
2. 置閏案例：
   * `2027-12-11T12:00:00+08:00`
   * 顯示大雪（置閏）/ 上元 / 陰遁 / 四局
   * 顯示 notes
   * 顯示缺盤 fallback
3. 超出資料範圍：
   * 若 UI 可輸入 1800 年
   * 顯示奇門資料無法查詢 fallback
   * 頁面不崩潰

### 9.4 不該改的地方

檢查：

* 金函玉鏡區塊不受影響。
* 九宮飛星不受影響。
* 干支曆基本資料不受影響。
* 董公擇日隱藏狀態不受影響。
* `2Drive.ffs_gui` / `Note.txt` 不加入版控。

## 10. 第 72 包建議實作範圍

建議第 72 包允許修改：

* `src/main.js`
* `styles/main.css`
* `tests/run-tests.js` 僅在需要新增 UI helper tests 時

建議第 72 包不要修改：

* `src/qimenResolver.js`
* `src/qimenPlateLookup.js`
* `data/qimen/plates/**`
* `data/qimen/qimen_yuan_ju_table.json`
* `data/solar_terms_1899_2101.json`
* docs，除非第 73 包再補

建議第 72 包新增 / 使用 helper：

* `formatQimenJuLabel(ju)`
* `formatQimenSolarTermLabel(result)`
* `renderQimenSection(calendarResult or dateTimeText)`

但請依目前 `src/main.js` 結構調整，不強制拆太多。

## 11. 後續路線

建議：

1. 第 72 包：實作 UI 定局摘要 + 缺盤 fallback。
2. 第 73 包：docs 補 UI 接入實作結果。
3. 第 74 包：如果 UI 穩定，再設計手動覆寫遁別 / 局數。
4. 第 75 包以後：盤面 schema / 真實 1080 盤面資料填入策略。

不建議：

* 不建議第 72 包同時做手動覆寫。
* 不建議第 72 包同時做真實九宮盤渲染。
* 不建議第 72 包改 resolver。
* 不建議第 72 包改 plate JSON。

## 12. 本文件結論

第 72 包可以開始接前端，但第一版只接定局摘要與缺盤 fallback。UI 資料來源建議是 `resolveQimenJuFromFullTermCycleDraft(...)`，盤面 fallback 來源是 `getQimenPlate(...)`。

目前不渲染真實盤面，目前不做手動覆寫。formatter / plate lookup 均不回寫彼此結果。第 72 包應保持小步、低風險。

## 13. 第 72 包 UI 實作結果

第 72 包已完成前端「奇門遁甲」UI 區塊：

* 修改 `src/main.js`
* 修改 `styles/main.css`
* 新增前端「奇門遁甲」區塊
* 區塊動態插在金函玉鏡區塊後面
* 使用 `resolveQimenJuFromFullTermCycleDraft(...)` 取得奇門定局摘要
* 使用 `getQimenPlate(...)` 取得 plate lookup 結果
* 顯示欄位：
  * 奇門節氣
  * 元別
  * 遁別
  * 局數
  * 時柱
  * 狀態
  * 必要時顯示實際節氣
  * 必要時顯示備註
* `isIntercalary === true` 時，奇門節氣顯示 `大雪（置閏）` 這類格式
* plate `nullPlate` 等缺盤狀態顯示：
  * `盤面資料尚未建立，目前僅顯示定局結果。`
* formatter error 顯示：
  * `奇門遁甲資料目前無法查詢此時間。`
* plate lookup error 顯示：
  * `奇門盤面資料讀取失敗，暫時無法顯示盤面。`

第 72 包未做：

* 未做真實九宮盤面渲染
* 未做手動覆寫遁別 / 局數
* 未顯示 lookup metadata
* 未顯示 cache stats
* 未修改 resolver
* 未修改 plate lookup helper
* 未修改 data JSON
* 未修改 1080 plate skeleton
* 未修改 tests

### 13.1 code change summary

`src/main.js`：

* import `getQimenPlate`
* import `resolveQimenJuFromFullTermCycleDraft`
* 新增 `QIMEN_JU_LABELS`
* 新增 fallback message constants：
  * `QIMEN_MISSING_PLATE_MESSAGE`
  * `QIMEN_FORMATTER_ERROR_MESSAGE`
  * `QIMEN_PLATE_LOAD_ERROR_MESSAGE`
* 新增 `createQimenSection()`
* 新增 `insertQimenSection(section)`
* 新增 `renderQimenSection(dateTimeText)`
* 新增 `clearQimenSection()`
* 新增 `createQimenSummaryRows(qimen)`
* 新增 `createQimenSummaryRow(label, value, className)`
* 新增 `formatQimenSolarTermLabel(qimen)`
* 新增 `formatQimenJuLabel(ju)`
* 新增 `formatQimenNotes(notes)`
* 在 render flow 中呼叫 `renderQimenSection(dateTimeValue)`
* 在 clear flow 中呼叫 `clearQimenSection()`

`styles/main.css`：

* 新增 qimen heading / subtitle 樣式
* 新增 qimen summary / row / label / value 樣式
* 新增 qimen fallback 樣式
* 第 72 包初版使用 summary + fallback 呈現

## 14. 第 72A 包 layout 微調結果

使用者畫面檢查回饋：

* 第 72 包初版畫面中，定局摘要資訊被左右平均分散。
* 使用者希望：
  * 左側 1/3 或左半邊顯示奇門定局摘要
  * 右側 2/3 或右半邊預留未來盤面資訊
  * 目前右側顯示缺盤 fallback
* 因此第 72A 包調整為：
  * 左側：定局摘要
  * 右側：盤面區 / 缺盤 fallback

`src/main.js` layout 改動：

* `createQimenSection()` 改為建立：
  * `qimen-body`
  * `qimen-summary-panel`
  * `qimen-summary`
  * `qimen-plate-panel`
  * `qimen-plate-title`
  * `qimen-fallback`
* DOM 結構從原本：
  * `section -> heading, summary, fallback`
* 改為：
  * `section -> heading, body`
  * `body -> summaryPanel, platePanel`
  * `summaryPanel -> summary`
  * `platePanel -> plateTitle, fallback`

`styles/main.css` layout 改動：

* 新增 / 調整 `.qimen-body`
  * 桌面版採 `minmax(180px, 1fr) minmax(0, 2fr)`
  * 形成約 1:2 的左摘要 / 右盤面比例
* `.qimen-summary` 改為單欄
* 新增 `.qimen-summary-panel`
* 新增 `.qimen-plate-panel`
* 新增 `.qimen-plate-title`
* `.qimen-fallback` 改為右側 plate panel 內的文字提示
* 760px 以下 `.qimen-body` 改為單欄

第 72A 包 smoke：

* 桌面版已調整為左側定局摘要、右側「盤面」fallback 區
* 桌面 grid 量測約為 `264px 528px`
* 手機版改成單欄顯示
* 符合使用者截圖回饋方向

## 15. 第 72 / 72A 驗證結果

第 72 包驗證：

* `npm test`：通過
* `git diff --check`：通過
* `node --check src/main.js`：通過
* `node --check tests/run-tests.js`：未執行，因為未修改 tests

第 72 包 smoke：

| 查詢時間 | 結果 |
|---|---|
| `2027-12-26T12:00` | 通過，顯示冬至 / 上元 / 陽遁 / 一局 / 正常 / 缺盤 fallback |
| `2027-12-11T12:00` | 通過，顯示大雪（置閏） / 上元 / 陰遁 / 四局 / 置閏 / 備註 / 缺盤 fallback |

第 72A 包驗證：

* `npm test`：通過
* `git diff --check`：通過
* `node --check src/main.js`：通過

第 72A layout smoke：

* 桌面版左側定局摘要、右側「盤面」fallback 區
* 手機版單欄
* 未顯示 lookup metadata / cache stats
* 未做手動覆寫
* 未做九宮盤面

## 16. 目前 UI 狀態

目前奇門 UI 已有：

* 「奇門遁甲」區塊
* 副標：「置閏法定局」
* 左側定局摘要
* 右側盤面區
* 右側目前顯示缺盤 fallback
* 使用 full cycle draft formatter
* 使用 qimen plate lookup helper
* `nullPlate` fallback 正常
* 置閏顯示正常
* notes 顯示正常
* 實際節氣與奇門節氣不同時可顯示實際節氣

目前仍未有：

* 真實九宮盤面
* 盤面 object schema
* 手動覆寫
* lookup metadata debug panel
* cache stats panel
* method switch
* 真太陽時
* 盤面資料填入流程

## 17. 後續建議

建議下一步：

1. 先 commit 第 72～73 包。
2. 第 74 包：手動覆寫遁別 / 局數 UI 設計文件。
   * 先設計，不直接實作。
   * 手動覆寫只影響 plate lookup effective input。
   * 不回寫 formatter result。
3. 第 75 包：手動覆寫 UI 實作。
   * 如果使用者確認需要。
4. 之後再做：
   * 盤面資料 schema
   * 真實 1080 盤面資料填入策略
   * 九宮盤面 rendering
   * 盤面資料維護工具

不建議：

* 不建議下一包直接做真實九宮盤面。
* 不建議下一包直接改 resolver。
* 不建議下一包直接改 plate JSON。
* 不建議把手動覆寫和真實盤面一起做。
* 不建議現在加入拆補法 / 茅山法 / 無閏法 / method switch。

## 18. 第 72～73 包結論

第 72 包已完成奇門 UI 第一版接入。UI 已可顯示 full cycle draft formatter 定局摘要，也已可接 qimen plate lookup helper。目前 plate skeleton 為 null，因此顯示缺盤 fallback。

第 72A 包已依使用者截圖回饋調整 layout：左側定局摘要，右側盤面 fallback。目前未做真實九宮盤面、未做手動覆寫、未顯示 debug metadata。

下一階段建議先設計手動覆寫 UI，或暫停奇門 UI 進一步擴充，改整理階段交接摘要。
